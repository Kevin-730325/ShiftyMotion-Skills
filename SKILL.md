---
name: shifty-motion
description: "制作、优化和高保真复刻软件 UI 演示动效：分析参考片、还原真实矢量界面、丝滑打字、按钮光效、甩镜与三维层次。支持 AE 可编辑工程和 MP4，以及 HTML/Canvas/Remotion 可播放与逐帧实现，按交付目标选择路径。"
metadata:
  version: "3.3.0"
---

# ShiftyMotion Skills

让观众能追踪「操作 → 反馈 → 结果 → 下一步」。先对齐构图、对象交接与运动节奏，再处理材质、模糊和声音。v3 保留网页与真实 UI 的方法，加入经过 AE 成片迭代的镜头、光效与参考复刻流程；证据和适用边界见 [v3 制作经验](references/v3-production-notes.md)。

## 确定目标与实现路径

沿用用户已经确认的时长、文案、素材和品牌。区分自由创作、借鉴风格与高保真复刻；用户说「几乎一样／1:1」时，按参考重建具体状态与镜头，不能只做同类风格。用户指定软件时使用该软件；只要求更新技能时不另外制作影片。

- **高保真参考复刻：** 先读 [参考拆解与对照](references/reference-matching.md)。先做要求的片段，例如前 10 秒，不扩展成较长的近似版。
- **AE 工程、复杂摄像机与多层光效：** 读 [AE 制作与交付](references/after-effects.md)。需要 MP4 时真正渲染；不能以网页或外部合成视频冒充 AE 原生工程。
- **网页交互、快速迭代、程序化帧渲染：** 读 [HTML 运行与同步](references/html-runtime.md)，使用 `assets/web-motion/` 和 `scripts/serve_preview.py`。Remotion 沿用秒制时间表；帧采样来自 `frame / fps`。
- **甩镜、纵深、logo 和按钮光效：** 读 [镜头与光效](references/camera-and-light.md)。共享视觉规则，AE 与网页的具体实现分别处理，不要求两个引擎用相同参数。
- **打字、菜单与拖拽：** 读 [交互节奏](references/typing-and-interaction.md)。一般动作可查 [动作配方](references/motion-recipes.md)，按需选，不凑齐整套效果。
- **声音：** 读 [音效设计](references/sfx-design.md)。优先从随包 83 个 WAV 中按事件选择，索引在 `assets/sfx/manifest.json`，试听在 `assets/sfx/index.html`；组合与 AE／网页同步见 [配声配方](references/sfx-cue-recipes.md)。缺少合适声音时再读 [在线补充与来源](references/sfx-sources.md)，不把联网变成默认前提。
- **历史参考图证：** 按动作查 [8 段模式](references/motion-patterns.md)、[16 段分析](references/higgsfield-16.md)、[帧条索引](references/claude-strips.md)，不用默认全部载入。
- **其他编辑器：** 使用当前可用的官方工具；ChatCut JSX 用可用的 create-motion-graphics 技能。不要因本技能要求而安装不相关插件。

## 开始前：只补齐影响制作的选择

先读 [需求收集与 UI 选择](references/intake-and-ui.md)。从用户消息、附件和既有确认中提取已知项，不重复问。需要展示操作界面且 UI 真实性尚未明确时，**必须在采集、重绘和动画制作前**确认：严格使用真实 UI、允许视觉重建、或允许概念设计。这项选择影响素材需求、可编辑性和工作量；明确选择后不再额外设置审批关卡。

区分高保真复刻、仅借鉴风格与无参考原创。可直接给用户 [两套可复制模板](references/prompt-templates.md)，但不要求填满所有字段。未决选择合并成简短问题，允许“你来决定”；在答案到达前可整理已提供素材，不擅自开始依赖该答案的工作。

背景读 [背景选择与运动](references/backgrounds.md)：用户指定素材优先；否则询问已有偏好、随包库、原创或外部素材。确定静止还是运动、速度与效果。用户授权自行设计时，按阅读强度选择并简述，不强迫停下来审批。随包有 15 张独立静态背景、索引与预览，**不把联系表当背景、不把静态图称为可分层工程**。

## 素材与图层的真实性

1. 优先用提供的 UI kit、网页原始 SVG、字体和真实录屏。logo、图标要矢量时保留路径；原生文字保留可编辑性。截图、摄影和生成背景仍是位图，不能因包进 SVG/PDF 就称为真矢量。
2. 分清原始矢量、原生文本、参考重建轮廓、截图切片与示意界面。字体找不到时先比对字形、字宽和字距；短标题可重建轮廓，但注明非原始字体。不要把近似字体直接当作已还原。
3. 只拆需要运动的层：输入壳、正文、附件、按钮、菜单、结果、指针，以及必要的金属边、网格和柔光。保留原 UI 坐标；目标、高亮、裁切、光效共享局部坐标与父级变换。
4. UI 内部文本、参数与用户提供结果按真实来源保留。参考中的 Working／代码日志可在明确的概念复刻中使用，但不能宣称是目标产品真实处理流程；真实产品教程不得编造功能和成功状态。
5. 品牌替换保留目标 logo 几何，在原参考构图中做光学校准。开场品牌、文案按当次要求；不自动继承别的项目的 Higgsfield 或 目标品牌 标志。

## 编排与运动质量

可用 [事件表](assets/beat-sheet-template.md) 记录每个交接的触发、保留对象、进出对象、落点、镜头和声音，不要求用户先审批表格。

- **连续但有阅读落点：** 紧凑宣传片中避免一进入状态就完全静止。用减速后的轻推、侧移、纵向浏览或下一动作的预备持续推进；重要文字仍需可辨认。教程、暂停和用户指定静帧可以静止，不把持续漂移当作全场硬规则。
- **镜头有速度层次：** 分开预备、加速甩动、短过冲、回收和慢速跟随。不要每段都同样 ease-in-out、停稳、再启动。对象交接保留位置、大小或主方向线索，不能靠闪白掩盖跳位。
- **适量三维：** 用真实 Z 间距、遮挡、阴影和视差说明层级；前景 UI 抬起、背景后退。不是勾选 3D 后所有层仍在同一平面，也不是把整屏反复倾斜。
- **错峰与交叠：** 主体先动，次要元素按阅读顺序跟进；新对象可辨认后退出旧对象。压缩空等，不把整片简单倍速。细分字符要保持排版，避免逐字跳动造成呆板感。
- **反馈有状态链：** 普通点击轻压／变亮；关键按钮可压缩回弹、图标变化、局部光晕、点阵扫光再接光环转场。具体阶段服从参考，不把复杂按钮简化为大圆加光，也不让每次点击都爆光。
- **模糊跟运动走：** 快速段有方向拖影，落点清晰；文字柔光保留清晰核心。运动模糊、镜头景深、材质 bloom 是三件不同的事，不能相互替代。
- **同一舞台：** 背景用可复用的低对比点阵、细网格、暗角或缓慢波动光，留出主体空间。沿用用户认可的背景，不每幕另换一套装饰。

## 时间与声音

关键事件共用秒制时间表：文字增长、光标、按钮反馈、相机、光效、结果和 SFX 对齐。AE 用关键帧／表达式／标记承载；网页用可随机 seek 的 `render(t)`；渲染按帧采样，不能依赖实时播放积累状态。

键击短簇对应实际文字增长，停顿停止；点击声对齐按下，落位声对齐接触；运动声按素材内部峰值对齐主要速度段。文件头、波形峰与视觉触点分开记录，不把文件从第 0 秒直接对齐每个动作。教程保护旁白清晰，广告按已定音乐节奏安排。说明声音是原片复用、已生成还是重新设计，不把波形／音轨存在当作试听通过。

## 验证与交付

1. 先看原速因果和节奏，再查关键状态与转场前后帧；高保真任务提供同时间参考对照。修正错误字形、错位裁切、光效漏出圆角、标题残留、过厚光环和最终结果闪黑，再加装饰。
2. AE 检查素材、字体、表达式、相机与模糊实际生效；表达式在相关镜头时段和边界取样，不只查单一时点。网页检查播放、暂停恢复、随机 seek、重播、音频开关、全屏和尾部同步。
3. 技术检查验证最终导出的实际文件：完整解码、时长、尺寸、帧率、帧数和音轨。高帧率不等于动作丝滑；联系表不能替代原速观看。若只完成抽样或没有试听，明确检验范围。
4. 按请求交付 MP4、可编辑 AEP／源代码与必要素材；整理相对路径和字体／插件依赖，保持旧版可恢复。包内脚本与最终工程一致，修改不能只留在未打包的补丁里。
5. 没有原工程不承诺逐像素一致。区分已匹配的动作、刻意替换的品牌与内容、仍存在的材质／镜头差异。分享技能只包含可移植说明、工具和必要分析图，不附带私人会话、人物照片、完整影片或机器专属依赖。
